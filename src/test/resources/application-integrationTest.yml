spring.datasource.sits:
  driver-class-name: org.h2.Driver
  jdbc-url: jdbc:h2:mem:sits;MODE=ORACLE;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=FALSE;INIT=CREATE SCHEMA IF NOT EXISTS INTUIT
  schema: INTUIT

spring.elasticsearch.rest:
  uris: http://127.0.0.1:9201
